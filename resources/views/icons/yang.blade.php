<svg class="wuji-icons" viewBox="0 0 24 24" >
	<g id="yang" class="symbol">
		<title>yang</title>
		<desc>yang is the positive, strong, white</desc>
		{{-- {{ dd($test) }} --}}
		{{-- @foreach ($bin as $b)
				{{ $b }}
		@endforeach --}}
		<rect class="icon-element" x="0" y="10" width="24" height="4" shape-rendering="crispEdges" />
	</g>
</svg>
